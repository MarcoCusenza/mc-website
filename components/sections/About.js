import styles from "../../styles/sectionstyle/about.module.scss";

export default function About() {
  return (
    <section id="about" className={styles.aboutMe}>
      <div className={styles.description}>SU DI ME</div>
    </section>
  );
}
